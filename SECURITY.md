# セキュリティポリシー (SECURITY.md)

## このドキュメントについて

PoohMaは、家族のアカウント情報・パスワードヒントを扱うアプリケーションです。
脆弱性の報告は、ユーザーの安全に直結する重要な貢献として歓迎します。
本ドキュメントは、脆弱性を発見した方への報告方法の案内と、調査時に守っていただきたいルールをまとめたものです。

## 対応スコープ

本ポリシーが対象とするのは、現在本番稼働しているPoohMaアプリケーション（Webフロントエンド／Convexバックエンド）です。
バージョンごとの個別サポートは行っておらず、常に最新の本番デプロイのみを対象とします。

### 対象内（In Scope）

- 本番環境のPoohMaアプリケーション（フロントエンド／Convexバックエンド／認証フロー）
- 暗号化・鍵管理ロジック（`apps/web/src/lib/crypto.ts`, `apps/web/src/lib/biometric.ts` 相当）
- 認可・アクセス制御ロジック（`apps/web/convex/customBuilders.ts`, `apps/web/convex/rls.ts` 相当）
- OGP取得等、外部リクエストを発生させる機能（SSRF関連）
- CSVインポート／エクスポート機能

### 対象外（Out of Scope）

- Firebase、Convex Cloud、Vercel、Resend、microCMSといった、PoohMaが利用する第三者サービス自体の脆弱性（各サービスの報告窓口へ直接ご連絡ください）
- 事前許可のない負荷試験・DoS/DDoS攻撃の実施
- ソーシャルエンジニアリング（開発者・運用者本人への詐称連絡等）
- 物理的な攻撃（オフィス・端末への物理アクセスを前提とするもの）
- 自動スキャナによる、他ユーザーの利用体験を損なう大量アクセス

## 脆弱性の報告方法

- 報告先：`poohma@mail.ciderlabs.link`
- 脆弱性は、公開のGitHub Issueやプルリクエストではなく、上記の非公開窓口へご連絡ください。
- 報告には、可能な範囲で以下を含めてください。
    - 再現手順（できればPoC）
    - 想定される影響範囲（例：他ユーザーのヒントの窃取、認可バイパス等）
    - 影響を受けるコンポーネント・URL・関数名
- 実際のユーザーデータ（他人のパスワードヒント等）を報告に含めないでください。調査は必ずご自身のテスト用アカウント・テスト用家族グループの範囲で行ってください。

## 対応の目安

個人プロジェクトのため、対応時間のお約束はできませんが、いただいたご指摘は5営業日を目安に受領確認いたします。
深刻度が高い（マスターキーや他ユーザーのヒントの窃取につながる等）場合は、文面に明記いただけると助かります。

## 安全な調査のためのルール（セーフハーバー）

以下のルールに従った調査行為については、法的措置の対象としないことを約束します。

- 調査は自分のテストアカウント・テスト用家族グループの範囲にとどめること
- 他ユーザーのデータへ意図せずアクセスしてしまった場合は、直ちに調査を中止し、アクセスしたデータを保持・閲覧・共有せずに報告すること
- 本番環境のデータを破壊・改ざんする操作を行わないこと
- 公表は、修正のリリース後、または報告から90日が経過するまで控えること（協調的開示）

## 既知の設計上の非対象事項

以下はPoohMaの設計上、意図的に「守らない」としている事項です。
脆弱性としてではなく、設計判断として扱います。
（詳細は脅威モデルドキュメントを参照）

- パスコードを紛失し、かつリカバリーキーも設定・保管していない場合のデータ復旧（E2EEの設計上の帰結）
- サービス名・URL・ログインID・メモ・タグ等のメタデータの秘匿（暗号化対象はパスワードヒントのみで、これらは平文でサーバーに保存されます）
- XSSが成立し、ブラウザ上で任意のJavaScriptが実行可能になった後の、展開済みマスターキー・表示中の平文ヒントの漏洩防止（E2EEはクライアント実行環境の健全性を前提とするため、XSS成立後の情報漏洩は技術的に防止できない）

ただし、XSS脆弱性そのものの発生（入力値のサニタイズ不足、出力エスケープ漏れ、CMS由来コンテンツの未検証な描画等）および、XSSを助長する設計上の欠陥（CSP未設定、Trusted Types未使用等）は、明確に報告対象スコープ内です。

## 謝辞

責任ある開示にご協力いただいた方は、ご本人の同意のもと、本ファイルまたはリリースノートにお名前を記載します。
